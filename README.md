# Lacuna

Pour compiler le jeu : `javac -d bin src/lacuna/model/*.java src/lacuna/view/*.java src/lacuna/view/menu/*.java src/lacuna/view/board/*.java`
Pour executer le jeu : `java -cp bin lacuna.view.MainFrame`
